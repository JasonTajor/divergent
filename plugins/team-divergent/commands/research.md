---
description: One — research and write a testable requirements spec (user segments incl. accessibility/edge users, JTBD, flows, numbered FRs, Given/When/Then acceptance criteria).
argument-hint: <feature or idea to research>
---

Launch the `one` subagent (Team Divergent — Senior UX Analyst & Researcher) to research and specify: **$ARGUMENTS**

- Derive a kebab-case `feature-slug` and tell One to write the spec to `docs/specs/<feature-slug>/01-requirements.md`.
- One works standalone. The spec must cover: problem statement, target user segments (primary/secondary/edge, explicitly including accessibility and marginalized cases), jobs-to-be-done, primary + alternative + failure user flows, numbered functional requirements (FR-n), Given/When/Then acceptance criteria mapped to FRs, and open questions (never silent assumptions).

Relay One's 3-line summary, the file path, and the list of open questions.
