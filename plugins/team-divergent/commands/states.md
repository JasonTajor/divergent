---
description: Dauntless — add the missing UI states (loading, empty, error, success, disabled) to a component or view, with real copy — no more happy-path-only screens.
argument-hint: <component or view to complete>
---

Launch the `dauntless` subagent (Team Divergent — Senior Frontend Engineer) to complete the states of: **$ARGUMENTS**

- Implement every state the screen can be in: **idle, loading** (skeleton/spinner), **empty** (teaches what to do, doesn't apologize), **error** (says what happened AND what to do next, with a retry path), **success**, and **disabled**.
- Wire all async outcomes to the data layer; use **real microcopy** (never lorem ipsum); use palette tokens and the 8pt grid.
- **Verify:** exercise each state (mock the data layer if needed), run lint + build.

Boundaries: frontend files only. Relay which states you added.
